/** Default hashing algorithm */
const HASH = 'sha256'

/** Hash length in bytes */
const HASH_SIZE = 32

/** Default block size to build the tree hash */
const BLOCK_SIZE = 4096

module.exports = {
    HASH, HASH_SIZE,
    BLOCK_SIZE,
}
